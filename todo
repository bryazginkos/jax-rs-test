# Сделать локализацию для bean validation с параметрами # Выставить ограничение на длинну текстовых полей.
# Написать кастомный валидатор на поле с датой (не раньше лет назад, количество лет задается параметром)
# Сделать обновление через PUT
# При пагинации возвращать не только список контактов, но и информацию о странице
# Если метод ничего не возвращает, используем 204.
# Добавить возможность фильтрации контактов по группе
# Сделать имя контакта уникальным и возвращать соответствующий код в случае попытки создать контакт с существующим именем.
# Подумать над возможной иерархией эксепшнов и мапперов. Сделать так, чтобы при ошибке возвращалась информация с ее деталями.
# В сервисном слое не должно быть DTO и конверторов. Вся работа с DTO должна быть в презентационном слое. Сервисный слой же должен принимать и возвращать доменные объекты.
# Неверное использование кода 202 при загрузке изображения. Подумать, как правильнее сделать загрузку изображений.